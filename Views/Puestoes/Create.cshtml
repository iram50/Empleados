@model CFE.Models.Puesto

@{
    ViewData["Title"] = "Crear Nuevo Puesto";
}

<div class="container">
    <div class="row mb-4">
        <div class="col">
            <h1 class="display-6">
                <i class="bi bi-briefcase"></i> @ViewData["Title"]
            </h1>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-header bg-cfe text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-plus-circle"></i> Información del Puesto
                    </h5>
                </div>
                <div class="card-body">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="form-group mb-4">
                            <label asp-for="DescripcionPuesto" class="form-label fw-bold">Descripción del Puesto</label>
                            <input asp-for="DescripcionPuesto" class="form-control" placeholder="Ingrese el nombre del puesto" />
                            <span asp-validation-for="DescripcionPuesto" class="text-danger small"></span>
                        </div>

                        <div class="d-flex justify-content-between mt-4 pt-3 border-top">
                            <a asp-action="Index" class="btn btn-outline-secondary me-3">
                                <i class="bi bi-arrow-left"></i> Volver al listado
                            </a>
                            <button type="submit" class="btn btn-cfe px-4">
                                <i class="bi bi-save"></i> Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .bg-cfe {
            background-color: #008a54 !important;
        }

        .btn-cfe {
            background-color: #008a54 !important;
            color: white !important;
            border: none !important;
        }

            .btn-cfe:hover {
                background-color: #006f45 !important;
                color: white !important;
            }

        .form-control {
            padding: 0.5rem 0.75rem;
            border-radius: 0.25rem;
            border: 1px solid #ced4da;
        }

            .form-control:focus {
                border-color: #008a54;
                box-shadow: 0 0 0 0.25rem rgba(0, 138, 84, 0.25);
            }

        .btn {
            padding: 0.5rem 1.25rem;
            margin: 0.25rem 0;
            font-weight: 500;
        }

        .btn-outline-secondary {
            border-width: 2px;
        }

        .card {
            border-radius: 0.5rem;
            border: none;
        }

        .card-header {
            font-weight: 600;
        }
    </style>
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}