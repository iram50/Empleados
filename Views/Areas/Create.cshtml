@model CFE.Models.Area

@{
    ViewData["Title"] = "Crear Nueva Área";
}

<div class="container">
    <div class="row mb-4">
        <div class="col">
            <h1 class="display-6">
                <i class="bi bi-building"></i> @ViewData["Title"]
            </h1>
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-header bg-cfe text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-plus-circle"></i> Información del Área
                    </h5>
                </div>
                <div class="card-body">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="form-group mb-4">
                            <label asp-for="DescripcionArea" class="form-label fw-bold">Descripción del Área</label>
                            <input asp-for="DescripcionArea" class="form-control" placeholder="Ingrese el nombre del área" />
                            <span asp-validation-for="DescripcionArea" class="text-danger small"></span>
                        </div>

                        <div class="d-flex justify-content-between mt-4 pt-3 border-top">
                            <a asp-action="Index" class="btn btn-outline-secondary me-3">
                                <i class="bi bi-arrow-left"></i> Volver al listado
                            </a>
                            <button type="submit" class="btn btn-cfe px-4">
                                <i class="bi bi-save"></i> Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .form-control {
            padding: 0.5rem 0.75rem;
            border-radius: 0.25rem;
        }

        .btn {
            padding: 0.5rem 1.25rem;
            margin: 0.25rem 0;
        }

        .btn-outline-secondary {
            border-width: 2px;
        }

        .card {
            border-radius: 0.5rem;
        }

        .bg-cfe {
            background-color: #008a54 !important;
        }

        .btn-cfe {
            background-color: #008a54 !important;
            color: white !important;
            border: none !important;
        }

            .btn-cfe:hover {
                background-color: #006f45 !important;
                color: white !important;
            }


    </style>
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}